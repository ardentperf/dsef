# dsef extension
#
# when changing the version make sure to also change in META.json and in dsef.sql (it's hard coded there on purpose)
# to minimize confusion, let's keep the PGXN distribution version the same as the extension version
#
comment = 'DiffStats and ExplainFull'
default_version = '2024.4.9'
relocatable = true
superuser = false
requires = plpgsql
